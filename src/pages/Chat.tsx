import React, { FC } from 'react';
import ChatWrapper from '../components/Chat/Chat'
const Chat:FC = () => {
  return (
    <div>
      <ChatWrapper></ChatWrapper>
    </div>
  );
};

export default Chat;